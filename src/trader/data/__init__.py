"""Data layer — CoinMarketCap Agent Hub (market data + x402) and BscScan (on-chain).

x402 pay-per-request lives in the CMC Agent Hub MCP, not the standalone `cmc` CLI.
BscScan provides wallet/transfer/holder data for on-chain signals and monitoring.
"""
